# Integration section is required to make use of the keys stored in 
# Codemagic UI under Apple Developer Portal integration.
workflows:
  ios-workflow:
    name: iOS Workflow
    environment:
      ios_signing:
        provisioning_profiles:
          - Shift7_Distribute_Certificate
        certificates:
          - Visual_Distribute_Certificate
    integrations:
      app_store_connect: moy
    scripts:
      - name: Configure Git and CocoaPods for better performance
        script: |
          git config --global advice.detachedHead false
          git config --global http.postBuffer 524288000
          git config --global http.lowSpeedLimit 0
          git config --global http.lowSpeedTime 999999
          git config --global http.version HTTP/1.1
          git config --global url."https://".insteadOf git://
          # Increase CocoaPods timeout
          export COCOAPODS_DISABLE_STATS=true
      - name: Get Flutter packages
        script: |
          flutter packages pub get
      - name: Prepare iOS dependencies
        script: |
          flutter precache --ios
          cd ios
          flutter pub get
          cd ..
      - name: Install CocoaPods dependencies
        script: |
          cd ios
          # Clean CocoaPods cache
          pod cache clean --all || true
          # Remove old Pods and lock file
          rm -rf Pods Podfile.lock .symlinks
          # Update CocoaPods repo (skip if fails, will retry during install)
          pod repo update || echo "Repo update failed, will continue..."
          # Install pods with multiple retries
          MAX_RETRIES=3
          RETRY_COUNT=0
          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            echo "Attempt $((RETRY_COUNT + 1)) of $MAX_RETRIES to install pods..."
            if pod install --repo-update; then
              echo "Pods installed successfully!"
              break
            else
              RETRY_COUNT=$((RETRY_COUNT + 1))
              if [ $RETRY_COUNT -lt $MAX_RETRIES ]; then
                echo "Pod install failed, retrying in 15 seconds..."
                sleep 15
              else
                echo "Pod install failed after $MAX_RETRIES attempts"
                exit 1
              fi
            fi
          done
          cd ..
      - name: Set Gradle properties for better memory management
        script: |
          echo "org.gradle.jvmargs=-Xmx10G -XX:MaxMetaspaceSize=5G -XX:ReservedCodeCacheSize=1G -XX:+HeapDumpOnOutOfMemoryError -XX:+UseG1GC" >> android/gradle.properties
          echo "org.gradle.parallel=true" >> android/gradle.properties
          echo "org.gradle.caching=true" >> android/gradle.properties
      - name: Set up keychain to be used for codesigning using Codemagic CLI 'keychain' command
        script: |
          keychain initialize
      - name: Use system default keychain
        script: |
          keychain add-certificates
      - name: Set up code signing settings on Xcode project
        script: |
          xcode-project use-profiles
      - name: Flutter build ipa and automatic versioning
        script: |
          export GRADLE_OPTS="-Xmx10G -XX:MaxMetaspaceSize=5G"
          flutter build ipa --release \
          --build-name=1.0.4 \
          --build-number=10 \
          --export-options-plist=/Users/builder/export_options.plist
    artifacts:
      - build/ios/ipa/*.ipa


    publishing:
      app_store_connect:
        # Use referenced App Store Connect API key to authenticate binary upload
        auth: integration
        submit_to_testflight: true
        beta_groups:
          - tester
          - Sufian
          - VISUAL


